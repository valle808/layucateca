import ContactClient from "@/components/ContactClient";

export const metadata = {
  title: "Contacto — La Yucateca || Contact Us — La Yucateca",
  description: "Ponte en contacto con La Yucateca. Pide una cotización o empieza tu proyecto hoy. || Request a quote or start your project.",
};

export default function ContactPage() {
  return <ContactClient />;
}
